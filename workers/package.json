{
  "name": "lunch-vote-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply lunch_vote --local",
    "migrate:remote": "wrangler d1 migrations apply lunch_vote --remote",
    "test": "vitest run",
    "teams:icons": "node ../scripts/generate-icons.mjs",
    "teams:zip": "node ../scripts/build-teams-zip.mjs",
    "teams:build": "npm run teams:icons && npm run teams:zip"
  },
  "dependencies": {
    "unpdf": "^0.12.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260421.1",
    "@types/node": "^20.19.39",
    "typescript": "^5.4.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.84.1"
  },
  "overrides": {
    "tar": "^7.5.11",
    "@mapbox/node-pre-gyp": "^2.0.0"
  }
}
